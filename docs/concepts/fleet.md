# Fleet

A *fleet* is the complete set of repositories associated with a GitHub user or organisation.

Nave treats this set as a structured dataset rather than a collection of isolated projects.

You can query the fleet for:

- configuration patterns
- dependency usage
- CI structure
- drift between repositories
