# Query language spec

## Syntax

value
scope:value
value|value

## Meaning

AND = space args
OR = `|`

## Scopes
repo, file, path (future)

## Usage
search, build, pen

## Rule
system-wide filter language, not just search
