# Pens

A *pen* is a scoped working set of repositories derived from a fleet query.

Pens allow you to:

- operate on subsets of repos safely
- run coordinated changes
- track state across multiple working trees

Pens are ephemeral in intent but persistent in structure.
