# ++"nave schemas list"++

List schemas and their cache status.

## Usage

```bash
--8<-- "docs/_snippets/cli/schemas/list.txt"
````

## Description

Displays all known schemas and whether they are cached locally.

Output format:

* `✓` — schema is cached
* `·` — schema is missing

Use `--json` for machine-readable output.
