# nave search

Search across cached repositories for patterns in tracked files.

## Usage

--8<-- "_snippets/cli/search.txt"

## Model

Search operates over structured scopes:

- file content
- configuration fields
- optional structural predicates

It is designed for cross-repository querying rather than single-repo search.
