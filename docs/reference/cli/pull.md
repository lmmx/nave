# nave pull

Sparse-checkout scanned repositories into a local cache.

## Behaviour

- Only tracked files are pulled
- Uses cached scan metadata
- Avoids full repository clones

This creates a working local representation of the fleet.
