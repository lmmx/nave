# nave scan

List a user's repositories and index tracked configuration files.

## Usage

--8<-- "_snippets/cli/scan.txt"

## Behaviour

- Only public repositories are scanned
- Results are cached locally
- Designed to be incremental by default

This is the entry point into the fleet model.
